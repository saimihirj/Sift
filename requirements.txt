# ── Core backend (lightweight, runs on Render Free 512MB) ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9
pydantic>=2.9.0
python-dotenv>=1.0.0
Authlib>=1.3.1
itsdangerous>=2.2.0

# ── LLM providers (API clients only, no local models) ──
ollama>=0.4.0
anthropic>=0.40.0

# ── Document parsing ──
python-docx>=1.1.0
pdfplumber>=0.11.0
python-pptx>=1.0.0
beautifulsoup4>=4.12.0
requests>=2.32.0

# ── HEAVY DEPS (disabled for free-tier cloud deploy) ──
# chromadb>=0.5.0
# sentence-transformers>=3.0.0
# libsql-experimental>=0.0.42
#
# Uncomment the above if you are self-hosting with >=2GB RAM
# and want RAG retrieval + Turso database support.
